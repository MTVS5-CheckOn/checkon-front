"use client";

import { cn } from "@/ui/utils/tailwind/cn";

export default function Page() {
  return (
    <div
      className={cn(
        // 1. Layout
        "flex h-screen w-full flex-col items-center gap-4 p-8",
        // 6. Utility
        "overflow-y-auto",
      )}
    >
      asd
    </div>
  );
}
