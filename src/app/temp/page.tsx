"use client";

import { cn } from "@/ui/utils/tailwind/cn";

export default function Page() {
  return (
    <div
      className={cn(
        "flex h-screen w-full flex-col items-center gap-4 overflow-y-auto p-8",
      )}
    >
      asd
    </div>
  );
}
